apiVersion: v1
kind: Service
metadata:
  name: ml-service
  namespace: cloudsentro
  labels:
    app: ml-service
spec:
  type: ClusterIP
  selector:
    app: ml-service
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
